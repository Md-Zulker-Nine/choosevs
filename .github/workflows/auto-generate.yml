name: Auto-Generate Content

on:
  schedule:
    # Blog posts: 3 times/day (every 8 hours)
    - cron: '0 */8 * * *'
    # Comparisons: Start conservative (5/day), scale up later
    - cron: '0 */4 * * *'
  workflow_dispatch:
    inputs:
      posts:
        description: 'Number of blog posts to generate'
        required: false
        default: '3'
      comparisons:
        description: 'Number of comparisons to generate'
        required: false
        default: '5'
      generate_images:
        description: 'Generate images for content'
        required: false
        default: 'true'

jobs:
  generate-posts:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: github.event.schedule == '0 */8 * * *' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Generate Blog Posts (3 posts, 1000+ words each)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          MAX_WORDS: 1500
        run: npx tsx scripts/autoblog/generate-posts.ts --count=${{ github.event.inputs.posts || '3' }} --min-words=1000

      - name: Commit and Push Blog Posts
        run: |
          git config user.name "ChooseVS Bot"
          git config user.email "bot@choosevs.com"
          git add src/pages/blog/ public/images/
          git diff --staged --quiet || git commit -m "Blog: ${{ github.event.inputs.posts || '3' }} posts ($(date +%Y-%m-%d))"
          git push

  generate-comparisons:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: github.event.schedule == '0 */4 * * *' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate Comparisons (conservative start)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: npx tsx scripts/autogenerate/generate-comparisons.ts --count=${{ github.event.inputs.comparisons || '5' }} --images

      - name: Commit and Push Comparisons
        run: |
          git config user.name "ChooseVS Bot"
          git config user.email "bot@choosevs.com"
          git add src/pages/compare/ public/images/
          git diff --staged --quiet || git commit -m "Compare: ${{ github.event.inputs.comparisons || '5' }} pages ($(date +%Y-%m-%d))"
          git push
